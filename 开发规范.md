# 开发规范 — 图片批量 AI 重命名器

## 1. 总体约束
- **语言**：Python 3.9+；核心引擎单文件可容（`core.py` ≤ 400 行）
- **依赖**：`Pillow`、`httpx`（或 `openai` SDK）、`rich`（可选）
- **结构**：
  - `main.py` — CLI 入口
  - `core.py` — 图片读取/压缩/LLM 调用/标签解析
  - `rename_rules.py` — 模板引擎与冲突处理
  - `undo.py` — rename_map 读写与回滚
- **禁止**：任何对源文件 delete/truncate 操作；密钥硬编码

## 2. CLI 接口（硬性）
```
picren --dir <path> [--recursive] [--pattern *.jpg|*.png]
       [--template "{date}_{category}_{subject}_{index}.{ext}"]
       [--tags-only] [--dry-run] [--execute] [--undo] [--move-by-category]
       [--keep-original] [--api-base URL] [--model name]
```
- 默认模式 = `--dry-run`（只预览）
- 真实执行必须显式 `--execute`（二次保险）
- `--tags-only`：只输出 `tags.csv`（文件名,日期,类别,主题,场景,置信度）不改名

## 3. 模板引擎语法
- `{date}` 原图拍摄日期（EXIF 优先，无则文件 mtime）→ `YYYY-MM-DD` 或自定义 `{date:%Y}`
- `{category}` 场景分类（旅行/美食/文档/宠物/人物/街拍/风景/其他）
- `{subject}` 主体名（LLM 生成中文 1-4 词，如"东京塔"）
- `{index}` 自增序号（01,02…）解决重名
- `{ext}` 原扩展名小写
- 非法字符过滤：`/\:*?"<>|` 与首尾点、空格

## 4. 重名与冲突策略
- 同目录重名 → 加 `_{index}` 后缀自动去重
- 目标名与现存文件重复 → 前缀 `dup_` 不覆盖（绝不覆盖已有文件）
- 单个文件失败（API 错误/非图片）→ 跳过并记录 `failed.csv`，不中断整批

## 5. 安全与隐私
- 默认只读原图；上传前用 Pillow 压缩至最长边 ≤800px、JPEG 质量 80
- 发送给 LLM 的 prompt 只含"压缩图 + 要求 JSON"；不传 EXIF/路径
- `rename_map.csv` 格式：`old_name,new_name,mtime`（UTF-8）
- `--undo` 读取 rename_map 逆序回滚（改名→原文件名，冲突时跳过）
- 提示：`.env` 管理 `API_KEY`，README 明示"图片会发送给第三方模型服务"

## 6. LLM 输出契约（JSON 严格解析）
LLM 必须返回（否则视为该张失败）：
```
{"subject": "东京塔", "category": "旅行", "scene": "夜晚", "confidence": 0.92}
```
- prompt 内给出封闭枚举 category，subject 限 4 词
- 解析失败重试 1 次；仍失败 → 保留原名并写 failed.csv

## 7. 测试
- `tests/test_rules.py`：模板渲染、非法字符、重名策略 ≥15 用例
- `tests/test_undo.py`：改名→回滚→原文件完好
- `tests/test_main.py`：CLI 参数与 dry-run 不产生改动（mock LLM）
- 用 `--dry-run` 对示例目录人工目验

## 8. 交付物
- 源码 + `tests/` 全绿
- README：安装、模板表、示例、隐私声明、免责
- `pyproject.toml`：`picren` 命令
- 一张 demo 动图（可选）