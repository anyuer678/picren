# 开发规划 — 图片批量 AI 重命名器

> 周期：3 天。MVP 后可在 2 天内加 GUI。

## Phase 1（4 小时）：核心引擎
**产出：`core.py` + `tests/`**
- [ ] 图片读取（PIL）→ 压缩样本 → LLM 视觉（OpenAI 兼容 API）返回 JSON 标签
- [ ] `rename_rules.py`：规则模板解析（`{date}_{category}_{subject}_{index}.{ext}`）
- [ ] 冲突处理（重名自动序号、异常隔离）
- [ ] `--dry-run` 预览模式
- 验证：`python main.py --dir sample --dry-run` 打印建议名列表（不动文件）

## Phase 2（4 小时）：CLI 完善
- [ ] 全参数：`--dir/--recursive/--pattern/--template/--ai-model/--tags-only`
- [ ] `--tags-only` 仅生成清单 CSV（供人工核对）
- [ ] 撤销机制：自动生成 `rename_map.csv`，可用 `--undo` 恢复
- [ ] 并发 API 请求（10 线程）+ 重试 + 限速
- 验证：真实照片文件夹跑通，100 张图 ≤ 10 分钟

## Phase 3（半天）：GUI（可选，追加）
- [ ] tkinter 或 Streamlit 单页：拖入文件夹 → 预览改名 → 应用
- [ ] 显示 AI 识别的标签置信度，允许人工改

## Phase 4（半天）：发布
- [ ] README（安装、示例、模板语法表、免责声明）
- [ ] `pyproject.toml`、`pip install -e .` 测试
- [ ] GitHub 仓库 + 示例截图

## 里程碑验收清单
- [ ] 自动命名符合模板且中文语义正确（`旅行_日本_东京塔_2026.jpg`）
- [ ] `--dry-run` 不产生任何文件改动
- [ ] 10 万文件不崩（批量模式下分块处理）
- [ ] 无 key 时给出提示而不是挂死

## 风险与对策
| 风险 | 对策 |
|---|---|
| 视觉模型识别错 | 保留原文件名前缀选项；`--keep-original` 硬性可选 |
| API 费用 | 图片压缩到 800px 以内再发；批内缓存相同标签 |
| 误改名 | 只改名不删文件；`--undo` 全量恢复 |
| 隐私 | 默认不上传原始图（压缩后发送），README 明示 |