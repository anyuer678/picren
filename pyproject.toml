[build-system]
requires = ["setuptools>=61"]
 build-backend = "setuptools.build_meta"

[pro ject]
name = "picren-ai"
version = "0.1.0"
de scription = "图片批量 AI 重命名/整理 器：AI 看图、改名、归档一条龙"
r eadme = "README.md"
requires-python = ">=3.9" 
license = { text = "MIT" }
dependencies  = [
    "Pillow==12.2.0",
    "httpx==0.28.1" ,
]

[project.optional-dependencies]
rich = [ "rich"]
dev = ["pytest"]

[project.scripts]
p icren = "main:main"

[tool.setuptools]
py-mod ules = [
    "main",
    "pipeline",
    "rea der",
    "vision",
    "rename_rules",
    " executor",
    "undo",
]

[tool.pytest.ini_op tions]
testpaths = ["tests"]
 