# 架构设计 — 图片批量 AI 重命名器

> 目标：管道式设计（读图→识别→命名→执行→回滚），各阶段干净接口、可插拔。

## 1. 总体分层

```
┌─────────────────────────────────────────────┐
│                main.py (CLI 装配)           │
├───────────┬────────────┬───────────────────┤
│ reader.py │ vision.py  │ rename_rules.py   │ 输入侧与规则
├───────────┴──────┬─────┴───────────────────┤
│        pipeline.py (编排：批处理+并发+重试) │
├──────────────────┼─────────────────────────┤
│ undo.py (rename_map 读写/回滚) │ executor.py │ 输出侧
└──────────────────┴─────────────────────────┘
```

依赖方向：`main → pipeline → reader/vision/rename_rules → undo/executor`。
**禁止**：`vision` 直接调用文件系统；`executor` 反向依赖 pipeline。

## 2. 接口契约

### 2.1 `reader.py`
```python
@dataclass
class ImageFile:
    path: str
    taken_at: datetime | None         # EXIF DateTimeOriginal；无则 mtime
    width: int; height: int
    ext: str

def collect_images(directory: str, *, recursive: bool,
                   patterns: list[str] = [".jpg",".jpeg",".png",".heic",".webp"]) -> list[ImageFile]
def read_preview(path: str, max_edge: int = 800) -> bytes
    # 压缩为 JPEG(质量80) 字节流；失败(非图片/损坏) -> raise ImageReadError
```

### 2.2 `vision.py`（所有模型调用收敛于此）
```python
REQUIRED_KEYS = ("subject","category","scene","confidence")

@dataclass
class VisionResult:
    subject: str        # 中文 ≤4词
    category: str       # 枚举之一
    scene: str
    confidence: float
    raw: dict

CATEGORIES = ("旅行","美食","文档","宠物","人物","街拍","风景","运动","建筑","其他")

class VisionError(Exception):  # 网络/解析失败

def analyze(preview_bytes: bytes, *, api_base: str, model: str,
            api_key: str, timeout_s: int = 30, retries: int = 1) -> VisionResult
    # 内部: build_vision_prompt() -> call -> parse_json(strict: REQUIRED_KEYS)
def build_vision_prompt() -> str        # 导出便于测试与调 prompt
def parse_json(text: str) -> VisionResult   # 兼容 markdown 包裹/extra fields
```

**LLM 视觉 JSON 契约**：
```json
{"subject": "东京塔", "category": "旅行", "scene": "夜晚", "confidence": 0.92}
```
`category` 必须命中枚举；`confidence` 0~1；解析失败→重试→VisionError（交由 pipeline 记 failed）。

### 2.3 `rename_rules.py`（纯函数）
```python
class TemplateError(ValueError)

def render_template(template: str, img: ImageFile, result: VisionResult,
                    index: int, index_pad: int = 2) -> str
    # {date}→2026-08-08, {date:%Y}→2026, {category},{subject},
    # {scene},{index}→01, {ext}→jpg（小写）; 未知占位符→TemplateError

def sanitize_name(name: str) -> str
    # 去掉 /\:*?"<>| 及首尾.和空格；保留中文

def resolve_unique(target_dir: str, desired: str, taken: set[str]) -> str
    # 已存在/待使用→拼接 _{index}；绝不覆盖
```

### 2.4 `executor.py`
```python
@dataclass
class RenameOp:
    src_abs: str
    dst_abs: str
    mtime_before: float

def apply_renames(ops: list[RenameOp], *, undo_map: str = "rename_map.csv") -> ApplyStats
    # 逐个 os.rename；失败逐个记录，不中断；成功后 append undo_map(UTF-8)
    # -> ApplyStats{renamed: int, failed: list[(src, err)]}

def dry_run_render(ops: list[RenameOp]) -> list[tuple[str, str]]
    # 纯展示，绝不触碰 fs
```

### 2.5 `undo.py`
```python
def load_map(path: str) -> list[RenameOp]
def reverse_ops(ops: list[RenameOp]) -> list[RenameOp]     # dst→src
def undo_from_map(path: str) -> ApplyStats                # 逆序执行，原子写回
```
回滚规则：目标不存在才回滚（已不存在→跳过记日志），避免覆盖他人文件。

### 2.6 `pipeline.py`
```python
@dataclass
class PipelineConfig:
    directory, recursive, patterns, template, dry_run_first: bool = True
    workers: int = 8, api_base, model, api_key, move_by_category: bool = False
    keep_original: bool = True, max_fail_ratio: float = 0.2

def run_pipeline(cfg: PipelineConfig) -> PipelineReport
    # PipelineReport: {planned: [...], executed: ApplyStats|None,
    #                 failed: list[(path, err)], tags: list[VisionResult],
    #                 elapsed_s: float}
```
内部：并发 `ThreadPool(workers)` 跑 `vision.analyze`；失败比例 `>max_fail_ratio` → 终止并报"识别质量过低"。

## 3. CLI 契约（与开发规范一致）
```
picren --dir <d> [--recursive] [--pattern] [--template "{date}_{category}_{subject}_{index}.{ext}"]
       [--tags-only] [--dry-run(默认)] [--execute] [--undo] [--move-by-category]
       [--keep-original] [--api-base/--model/--api-key(或env)] [--workers 4] [--json]
```
| 模式 | 说明 |
|---|---|
| 默认/--dry-run | 打印 src→dst 表与 tags，不改文件 |
| --tags-only | 只写 tags.csv，不 rename |
| --execute | 真实执行（先自动 dry-run 展示） |
| --undo | 读取最近 rename_map.csv 回滚 |

退出码：0 成功；1 参数错；2 无匹配文件；3 失败过多中止。

## 4. 数据流
```
main → pipeline.load+preview(并发) → vision.analyze(并行,重试) → VisionResult
 → render_template → resolve_unique_name → (dry_run 或 executor.apply)
 → undo_map 落盘
```

## 5. 并行开发切分
- **AI-A**：`reader.py` + `rename_rules.py` + 测试（先冻结类型）
- **AI-B**：`vision.py` 契约 + mock 模式（不真调 API 也能测）
- **AI-C**：`executor.py` + `undo.py` + 测试（重点：原子性/回滚）
- **AI-D**：`pipeline.py` 编排 + CLI（依赖 A/B/C 桩）
顺序：A/B/C 并行 → D 集成。

## 6. 接口核对清单
- [ ] `sanitize_name` 不产生非法文件名（含 emoji/中文测试）
- [ ] `resolve_unique_name` 永不冲突
- [ ] `undo_from_map` 还原文件字节一致
- [ ] `--dry-run` 后目录 mtime 未变（哈希校验可选）