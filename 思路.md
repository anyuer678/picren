# 图片批量 AI 重命名/整理器（PicRename AI）

> 一句话：把 `IMG_20230101_103055.jpg` 变成 `旅行_日本_东京塔_2026.jpg`——AI 看图、改名、归档一条龙。

## 一、项目思路

### 1. 要解决的问题
相机/手机导出的图片全是 `IMG_0001.jpg`、`Screenshot_...` 这类无意义文件名。整理相册要么全靠肉眼翻，要么靠 EXIF 时间猜。用户需要"这照片是什么"→ 中文语义化命名 + 自动分类。

### 2. 调研证据（2026年8月实搜）
- GitHub 全站搜"AI photo organizer rename"仅 4 个仓库，最高 657★（且是日记应用副产品），**无独立头部**
- 国内只有 Riffo（Mac 付费）、PhotoEasy 等小工具，无主导者
- 常见工具（Lightroom/Google Photos）只做"按日期分类"，不做"按内容命名"
- 隐私刚需：照片是最高隐私数据，本地处理是最强卖点

### 3. 用户画像
- 摄影师/旅行党（每年几千张照片）
- 家庭照片整理需求（爸妈手机里一堆"新建文件夹"）
- 自托管玩家（跑 Immich 但导入前想先整理）

### 4. 产品形态（MVP）
本地 CLI（Python）：
```
picren --dir /photos/2026 --template "{date}_{category}_{subject}_{index}"
# 输出：旅行_日本_东京塔_01.jpg / 家庭_生日蛋糕_02.jpg
```
可选 `--tags-only` 先生成标签清单人工确认再执行，**默认 dry-run 预览**。
可选 `--move-by-category` 按类别移入子文件夹。

### 5. 核心杀手锏（差异化）
1. **中文语义命名**：模板引擎 + 视觉模型标签（subject/category/scene/date）
2. **安全第一**：预览才执行、可一键 `--undo`、永不删文件
3. **本地优先**：压缩后样本才发送（默认 800px 内），可配完全本地模型（Qwen-VL/Ollama）

### 6. 边界与免责（低责任）
- 不碰人脸识别身份信息（不做"谁是谁"），只做内容标签
- 改名只是文件名层操作，不移动/不删除
- LLM 判断仅供参考，`--keep-original` 保证可回溯

## 二、技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 语言 | Python 3.9+ | PIL/tenacity 生态 |
| 视觉 | OpenAI 兼容多模态 API（DeepSeek-VL/通义/Qwen-VL 等）或本地 Ollama | key 自备，兼容国产 |
| 图像处理 | Pillow（压缩/EXIF 读取） | 标准 |
| 并发 | ThreadPoolExecutor | 100 张图 10 分钟内 |
| 输出 | JSON（标签）+ CSV（改名映射） | 可审计可撤销 |

## 三、开发规范

见 `开发规范.md`。核心硬性要求：
1. `--dry-run` 默认模式，没有确认不执行真实改名
2. 改名映射持久化到 `rename_map.csv`，`--undo` 可完整回滚
3. 只改文件名，绝不删除/移动原文件（除非显式 `--move`）
4. 上传前压缩图片至 ≤800px（隐私+成本）
5. 无 API key 时清晰报错并指引

## 四、开发规划（见 开发规划.md）

Phase 1 核心引擎 → Phase 2 CLI → Phase 3 可选 GUI → Phase 4 发布。

## 五、判断标准
- 100 张混合照片（风景/人物/美食/文档）自动命名，人工核对 ≥80% 合理
- 全程不删文件、可撤销
- 冷启动：`pip install` + 配 key = 开箱即用